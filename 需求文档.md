# 国网故障诊断系统 - 需求文档

## 文档信息

| 项目名称 | 国网故障诊断系统 (State Grid Fault Diagnosis System) |
|---------|--------------------------------------------------|
| 文档版本 | v2.0（基于实际代码） |
| 编制日期 | 2026-01-22 |
| 项目类型 | 电力设备智能诊断系统（纯前端应用） |
| 技术架构 | Vue 3 + TypeScript + Element Plus + ECharts |
| 文档说明 | 本文档完全基于现有代码编写，描述系统的真实功能和实际实现 |

---

## 1. 项目概述

### 1.1 项目背景

国网故障诊断系统是一个专为电力变压器故障诊断设计的前端Web应用，通过集成n8n工作流平台提供的API服务，实现了气体分析诊断、图像AI诊断和故障树可视化等核心功能。系统采用纯前端架构，部分功能使用localStorage进行数据持久化。

### 1.2 系统特点

- **纯前端应用**：基于Vue 3的单页应用（SPA），无需独立后端服务
- **API集成**：通过n8n webhook实现故障诊断和图像分析功能
- **本地存储**：使用localStorage存储诊断历史、图像任务等数据
- **可视化展示**：创新的逻辑门故障树可视化技术
- **响应式设计**：适配不同屏幕尺寸的企业级UI

### 1.3 核心价值

- 提供直观的故障诊断界面，降低操作门槛
- 集成多种诊断方法（三比值法、DPM分析、PRPD分析）
- 可视化展示故障逻辑关系，辅助快速定位问题
- 支持图像AI诊断，拓展诊断手段

---

## 2. 系统架构

### 2.1 技术架构

```
┌─────────────────────────────────────┐
│         前端应用 (Vue 3)             │
├─────────────────────────────────────┤
│  - UI组件层（Element Plus）          │
│  - 业务逻辑层（Services）            │
│  - 状态管理（Pinia）                 │
│  - 本地存储（LocalStorage）          │
└──────────────┬──────────────────────┘
               │ HTTP请求
               ├─────────────┐
               ↓             ↓
      ┌────────────┐  ┌────────────┐
      │ n8n故障诊断 │  │ n8n图像诊断 │
      │   Webhook  │  │   Webhook  │
      └────────────┘  └────────────┘
```

### 2.2 数据流向

1. **故障推理诊断**：前端表单 → n8n API → 文本响应 → 前端解析 → 结果展示
2. **图像AI诊断**：前端上传 → n8n API → localStorage任务管理 → 结果展示
3. **故障树管理**：纯前端内存操作，无后端持久化
4. **诊断历史**：localStorage读写，纯前端存储

---

## 3. 功能需求

### 3.1 故障推理诊断模块

#### 3.1.1 功能描述

提供基于溶解气体分析(DGA)的故障诊断功能，用户输入7种气体浓度参数后，系统调用n8n API获取诊断结果。

#### 3.1.2 当前实现

**页面组件**：`src/views/FaultDiagnosis.vue`
**服务层**：`src/services/diagnosis.ts`
**API集成**：`https://n8n.bd.kxsz.net:9443/webhook/power-fault-diagnosis-1`

#### 3.1.3 详细功能

**FR-101 气体参数输入**
- **输入字段**（7个必填项）：
  - H2_ppm：氢气浓度（ppm）
  - CH4_ppm：甲烷浓度（ppm）
  - C2H6_ppm：乙烷浓度（ppm）
  - C2H4_ppm：乙烯浓度（ppm）
  - C2H2_ppm：乙炔浓度（ppm）
  - CO_ppm：一氧化碳浓度（ppm）
  - CO2_ppm：二氧化碳浓度（ppm）
  - total_hydrocarbons_limit：总烃限值（可选）

- **输入方式**：
  - Element Plus数字输入框（type="number"）
  - 支持小数值
  - 预填充示例数据

- **默认值**：
  ```javascript
  H2_ppm: 50
  CH4_ppm: 100
  C2H6_ppm: 10
  C2H4_ppm: 100
  C2H2_ppm: 10
  CO_ppm: 100
  CO2_ppm: 100
  total_hydrocarbons_limit: 150
  ```

**FR-102 API诊断调用**
- **请求方式**：
  ```typescript
  POST https://n8n.bd.kxsz.net:9443/webhook/power-fault-diagnosis-1
  Content-Type: application/json
  Body: [{ H2_ppm: ..., CH4_ppm: ..., ... }]  // 数组包裹
  ```

- **响应处理**：
  - 返回纯文本格式（非JSON）
  - 前端使用`parseN8nTextResponse()`解析文本
  - 生成结构化数据对象
  - 提取关键指标

- **错误处理**：
  - HTTP错误：显示状态码和错误信息
  - 解析失败：提示用户诊断失败
  - ElMessage提示用户

**FR-103 诊断结果展示**
- **展示组件**：`EnhancedDiagnosisReport.vue`
- **展示内容**：
  - 原始文本响应（`raw_text`）
  - 解析后的结构化数据（`parsed_data`）
  - 诊断摘要（`summary`）
  - 关键指标（`metrics`）
  - 输入参数回显（`input_params`）

- **UI布局**：
  - 左侧：参数输入表单（10列）
  - 右侧：诊断结果展示（14列）
  - 响应式布局

**实现细节**：
```vue
<!-- 核心代码片段 -->
<template>
  <div class="fault-diagnosis-page">
    <ElRow :gutter="24">
      <!-- 左侧输入 -->
      <ElCol :span="10">
        <ElCard>
          <ElForm :model="manualParams">
            <ElFormItem label="H2 (ppm)">
              <ElInput v-model.number="manualParams.H2_ppm" type="number" />
            </ElFormItem>
            <!-- 其他气体输入... -->
            <ElButton @click="triggerDiagnosis">开始诊断</ElButton>
          </ElForm>
        </ElCard>
      </ElCol>

      <!-- 右侧结果 -->
      <ElCol :span="14">
        <EnhancedDiagnosisReport
          :report-data="currentDiagnosisResult"
          :is-loading="isLoading"
        />
      </ElCol>
    </ElRow>
  </div>
</template>

<script setup>
const triggerDiagnosis = async () => {
  isLoading.value = true;
  try {
    const result = await runFaultDiagnosis(manualParams);
    currentDiagnosisResult.value = result;
    ElMessage.success('诊断接口调用成功！');
  } catch (error) {
    diagnosisError.value = error.message;
  } finally {
    isLoading.value = false;
  }
};
</script>
```

---

### 3.2 图像AI诊断模块

#### 3.2.1 功能描述

基于深度学习的图像智能分析功能，支持上传PRPD、UHF、TEV、AE等类型的图像，通过n8n API进行AI分析，并展示诊断结果。

#### 3.2.2 当前实现

**页面组件**：`src/views/ImageDiagnosis.vue`
**服务层**：`src/services/imageDiagnosis.ts`（ImageDiagnosisService类）
**API集成**：`https://n8n.bd.kxsz.net:9443/webhook/upload-image`
**数据存储**：localStorage（key: `image_diagnosis_tasks`）

#### 3.2.3 详细功能

**FR-201 设备信息配置**
- **必填字段**：
  - device_id：设备编号（2-50字符）
  - device_type：设备类型（下拉选择）
    - 主变压器（main_transformer）
    - 配电变压器（distribution_transformer）
    - 开关柜（switchgear）
    - 断路器（circuit_breaker）
    - 互感器（instrument_transformer）
    - 其他（other）
  - location：设备位置（2-100字符）
  - image_type：图像类型（单选）
    - PRPD图
    - UHF图
    - TEV图
    - AE图
  - sensitivity：识别敏感度（低/中/高，默认中）

- **表单验证**：
  ```typescript
  formRules: {
    device_id: [
      { required: true, message: '请输入设备编号', trigger: 'blur' },
      { min: 2, max: 50, message: '设备编号长度在 2 到 50 个字符', trigger: 'blur' }
    ],
    device_type: [
      { required: true, message: '请选择设备类型', trigger: 'change' }
    ],
    location: [
      { required: true, message: '请输入设备位置', trigger: 'blur' },
      { min: 2, max: 100, message: '设备位置长度在 2 到 100 个字符', trigger: 'blur' }
    ]
  }
  ```

**FR-202 图像上传功能**
- **组件**：`ImageUploader.vue`
- **上传方式**：
  - 点击选择文件
  - 拖拽上传
  - 批量上传（多选）

- **文件限制**：
  - 支持格式：JPG、JPEG、PNG、BMP、TIFF
  - 单文件大小：理论无限制（根据浏览器）
  - 总文件数量：无限制

- **预览功能**：
  - 上传后即时预览
  - 显示文件名和大小
  - 支持删除已上传文件

**FR-203 任务提交和管理**
- **提交流程**：
  1. 表单验证通过
  2. 检查文件是否已上传
  3. 调用`ImageDiagnosisService.submitDiagnosisTask()`
  4. 生成唯一任务ID：`task_${timestamp}_${random}`
  5. 保存任务到localStorage
  6. 开始上传和处理

- **API调用**（真实模式）：
  ```typescript
  // 为每个图像文件创建FormData
  for (const file of params.image_files) {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('device_id', params.equipment_info.device_id);
    formData.append('device_type', params.equipment_info.device_type);
    formData.append('location', params.equipment_info.location);
    formData.append('image_type', params.image_type);
    formData.append('sensitivity', params.sensitivity);

    const response = await fetch(N8N_IMAGE_DIAGNOSIS_URL, {
      method: 'POST',
      body: formData
    });

    const result = await response.json() or response.text();
  }
  ```

- **任务状态**：
  - `pending`：等待处理
  - `uploading`：上传中（进度10-50%）
  - `processing`：处理中（进度60-95%）
  - `completed`：完成（进度100%）
  - `failed`：失败

**FR-204 任务进度追踪**
- **组件**：`TaskManager.vue`
- **实现方式**：
  - 每2秒从localStorage读取任务列表
  - 轮询机制：`setInterval(() => loadTasks(), 2000)`
  - 实时更新进度条和状态
  - 显示当前处理步骤

- **进度显示**：
  - 上传中：10-50%，显示"上传图像文件..."
  - 处理中：60-95%，显示处理阶段
    - 图像预处理（60-70%）
    - 特征提取与分析（70-85%）
    - AI模型推理（85-95%）
    - 生成诊断报告（95-100%）
  - 完成：100%，显示"诊断完成"

- **任务操作**：
  - 查看任务详情
  - 取消运行中的任务
  - 重试失败的任务
  - 删除单个任务
  - 清理所有已完成任务

**FR-205 结果展示**
- **组件**：`ResultDisplay.vue`
- **展示内容**：
  - 设备信息回显
  - 图像分析结果：
    - 放电类型（discharge_type）
    - 严重程度（severity_level：1-5）
    - 置信度分数（confidence_score：0-1）
    - 故障位置（fault_location）
    - 故障概率（fault_probability）
  - AI洞察（ai_insights）：字符串数组
  - 推荐措施（recommended_actions）：字符串数组
  - 处理后的图像（processed_images）：
    - 原始图像URL
    - 处理后图像URL
    - 标注信息（annotations）
  - 技术详情（technical_details）：
    - 算法版本
    - 模型置信度
    - 处理步骤

**FR-206 Mock模式**
- **启用条件**：`USE_MOCK_API = false`（默认关闭）
- **Mock实现**：
  - 模拟网络延迟（500ms）
  - 模拟任务处理过程（总计8-15秒）
  - 生成随机诊断结果
  - 支持5%概率的随机失败

**实现细节**：
```typescript
// 任务轮询
onMounted(() => {
  loadTasks();
  startTaskPolling();
});

const startTaskPolling = () => {
  taskPollingInterval.value = window.setInterval(() => {
    loadTasks();
  }, 2000);
};

const loadTasks = () => {
  const storedTasks = ImageDiagnosisService.getAllTasks();
  tasks.value = storedTasks;

  if (!currentTaskId.value && activeTasks.value.length > 0) {
    currentTaskId.value = activeTasks.value[0].task_id;
  }
};
```

---

### 3.3 故障树可视化模块

#### 3.3.1 功能描述

创新的逻辑门故障树可视化系统，支持AND/OR/NOT逻辑门的图形化展示，提供多种布局模式和丰富的交互功能。

#### 3.3.2 当前实现

**核心组件**：`src/components/logic/EnhancedFaultTree.vue`
**渲染引擎**：`src/utils/logicGateRenderer.ts`
**工作流解析**：`src/services/workflowParser.ts`
**预览页面**：`src/views/FaultTreePreview.vue`

#### 3.3.3 详细功能

**FR-301 逻辑门节点展示**
- **支持的逻辑门类型**：
  - AND门（&）：所有条件同时满足时输出为真
  - OR门（∨）：任一条件满足即输出为真
  - NOT门（¬）：条件取反

- **逻辑门状态**：
  - `true`：绿色显示
  - `false`：红色显示
  - `unknown`：灰色显示

- **节点样式**：
  - 不同逻辑门使用不同图形符号
  - 圆形边框，填充白色背景
  - 边框颜色根据逻辑门类型变化：
    - AND：蓝色（#409EFF）
    - OR：绿色（#67C23A）
    - NOT：橙色（#E6A23C）

**FR-302 故障节点展示**
- **节点信息**：
  - 节点ID（唯一标识）
  - 节点名称
  - 节点描述
  - 推荐措施（recommendation）
  - 关联参数

- **视觉表现**：
  - 矩形节点
  - 边框和背景颜色区分不同类型
  - 鼠标悬停高亮效果

**FR-303 布局算法**
- **支持的布局模式**：
  - **层次布局（hierarchical）**：
    - 传统的自上而下树形结构
    - 清晰的层级关系
    - 适合深层次故障树
  - **径向布局（radial）**：
    - 以根节点为中心的圆形分布
    - 子节点环绕父节点
    - 适合宽广型故障树
  - **紧凑布局（compact）**：
    - 网格式排列
    - 节省空间
    - 适合大规模故障树

- **布局切换**：
  - 下拉选择器切换布局
  - 切换时自动重新渲染
  - 重置视图位置和缩放

**FR-304 交互功能**
- **缩放控制**：
  - 缩放范围：20%-300%
  - 缩放方式：
    - 按钮控制（+/-）
    - 鼠标滚轮缩放
    - 重置按钮恢复100%
  - 显示当前缩放比例

- **拖拽平移**：
  - 鼠标左键按住拖拽
  - 光标变化：grab → grabbing
  - 平移偏移量实时更新

- **节点交互**：
  - 点击节点：打开侧边详情面板
  - 悬停节点：高亮效果
  - 节点详情：
    - 基本信息
    - 逻辑门类型
    - 当前状态
    - 判断条件
    - 推理过程

- **工具栏功能**：
  - 布局选择器
  - 缩放控制
  - 逻辑门显示/隐藏开关
  - 导出按钮（占位符）

**FR-305 图例显示**
- **位置**：左上角悬浮
- **内容**：
  - AND门符号和说明
  - OR门符号和说明
  - NOT门符号和说明
- **交互**：
  - 可展开/收起
  - 半透明背景
  - 毛玻璃效果（backdrop-filter）

**FR-306 侧边详情面板**
- **显示方式**：右侧抽屉（Drawer）
- **展示内容**：
  - 节点名称和类型标签
  - 逻辑门类型（带图标）
  - 当前状态（带颜色标签）
  - 判断条件（代码格式）
  - 节点描述
  - 子节点数量
  - 推理过程（logic_gate类型）
  - 诊断建议（fault_node类型）

**实现细节**：
```typescript
// 渲染核心逻辑
const renderTree = async () => {
  let faultTree: EnhancedFaultTreeNode | null = null;

  if (props.workflowData) {
    parsedWorkflow.value = parser.parseWorkflow(props.workflowData);
    faultTree = parsedWorkflow.value.fault_tree;
  } else if (props.faultTreeData) {
    faultTree = props.faultTreeData;
  }

  if (faultTree) {
    renderedTreeHTML.value = renderer.createHTMLStructure(
      faultTree,
      selectedLayout.value
    );
    await nextTick();
    attachEventListeners();
  }
};

// 缩放控制
const handleWheel = (e: WheelEvent) => {
  e.preventDefault();
  const delta = e.deltaY > 0 ? 0.9 : 1.1;
  const newZoom = Math.min(Math.max(zoomLevel.value * delta, minZoom), maxZoom);
  zoomLevel.value = newZoom;
};

// 拖拽控制
const handleMouseMove = (e: MouseEvent) => {
  if (isDragging.value) {
    const deltaX = e.clientX - lastMousePos.x;
    const deltaY = e.clientY - lastMousePos.y;
    panOffset.x += deltaX;
    panOffset.y += deltaY;
    lastMousePos.x = e.clientX;
    lastMousePos.y = e.clientY;
  }
};
```

---

### 3.4 故障树管理模块

#### 3.4.1 功能描述

提供故障树的创建、编辑、删除、克隆等全生命周期管理功能。**注意：此模块数据完全存储在前端内存中，刷新页面后数据重置为初始demo数据。**

#### 3.4.2 当前实现

**页面组件**：`src/views/FaultTreeManagement.vue`
**数据存储**：前端内存（`ref<FaultTreeData[]>`）
**初始数据**：3个demo故障树（包括1个专业示例）

#### 3.4.3 详细功能

**FR-401 故障树列表展示**
- **表格列**：
  - 选择框（支持多选）
  - 故障树名称（含版本号、模板标签）
  - 描述（支持Tooltip显示完整内容）
  - 状态标签（draft/active/archived/template）
  - 类别（变压器/开关/母线/保护系统）
  - 统计信息：
    - 节点数量
    - 逻辑门数量
    - 树深度
  - 标签（最多显示2个，超出显示+N）
  - 更新时间和更新人
  - 操作按钮

- **状态颜色**：
  - draft（草稿）：info蓝色
  - active（活跃）：success绿色
  - archived（归档）：warning橙色
  - template（模板）：primary蓝色

**FR-402 搜索和筛选**
- **关键词搜索**：
  - 搜索范围：名称、描述、标签
  - 模糊匹配
  - 实时搜索（输入即过滤）
  - 清空按钮

- **筛选条件**：
  - 状态筛选（多选）：draft/active/archived/template
  - 类别筛选（多选）：transformer/switch/busbar/protection
  - 设备类型筛选（高级）：power_transformer/distribution_transformer等
  - 时间范围筛选（高级）：日期范围选择器
  - 节点数量范围（高级）：滑块范围选择（0-200）

- **高级筛选面板**：
  - 可展开/收起（Collapse Transition）
  - 筛选条件保留

**FR-403 CRUD操作**
- **创建（Create）**：
  - 点击"新建故障树"按钮
  - 打开编辑弹窗（edit模式）
  - 填写基本信息：
    - 名称（必填，1-100字符）
    - 版本号（必填，如1.0.0）
    - 描述
    - 状态（draft/active/archived/template）
    - 故障类别（必填）
    - 设备类型（必填）
    - 标签（动态添加）
    - 是否设为模板
  - 切换到"节点编辑"标签页：
    - 添加根节点
    - 添加子节点
    - 添加兄弟节点
    - 编辑节点
    - 删除节点
  - 点击"创建"保存

- **读取（Read）/预览**：
  - 点击表格行或"预览"按钮
  - 打开预览弹窗
  - 显示故障树可视化（EnhancedFaultTree组件）
  - 支持布局切换（层次/径向/紧凑）
  - 支持缩放和交互

- **更新（Update）**：
  - 点击"编辑"按钮
  - 修改基本信息或节点
  - 自动更新统计信息（节点数、逻辑门数、树深度）
  - 更新时间戳和更新人

- **删除（Delete）**：
  - 点击下拉菜单中的"删除"
  - 确认对话框
  - 从内存列表中移除
  - **注意**：刷新页面后恢复初始数据

**FR-404 其他操作**
- **克隆**：
  - 完整复制故障树
  - 自动生成新ID
  - 名称添加"- 副本"后缀
  - 版本号重置为1.0.0
  - 创建/更新时间重置

- **导出**（占位符）：
  - 显示"导出功能开发中..."
  - 计划支持：JSON、XML、Excel、PDF

- **导入**（占位符）：
  - 显示"导入功能开发中..."

- **设为模板**：
  - 将`is_template`设置为true
  - 更新时间戳

- **归档/激活**：
  - 切换状态为archived或active
  - 更新时间戳

- **批量导出**：
  - 选中多个故障树
  - 批量导出功能（占位符）

**FR-405 节点编辑器**
- **节点操作工具栏**：
  - 添加根节点（仅当无根节点时可用）
  - 添加子节点（需先选中父节点）
  - 添加兄弟节点（需先选中节点，且非根节点）
  - 编辑节点（需先选中节点）
  - 删除节点（需先选中节点）

- **节点类型**：
  - 故障节点（fault_node）
  - 逻辑门节点（logic_gate）

- **节点属性编辑**：
  - 节点名称（必填）
  - 节点类型（必填）
  - 逻辑门类型（logic_gate时必填）：AND/OR/NOT
  - 描述
  - 判断条件（logic_gate专用）：
    - 支持的操作符：>, <, >=, <=, ==, !=
    - 支持的逻辑：AND, OR, NOT
    - 常用参数提示：H2_ppm, CH4_ppm等
  - 逻辑门状态（logic_gate专用）：true/false/unknown
  - 推荐措施（fault_node专用）

- **实时预览**：
  - 边编辑边预览故障树结构
  - 使用EnhancedFaultTree组件
  - 支持节点点击选中

**FR-406 初始Demo数据**
- **专业示例**：
  - 名称："本体异常产气故障诊断（专业示例）"
  - 状态：template
  - 节点数：21个
  - 逻辑门数：7个
  - 树深度：4层
  - 包含完整的诊断逻辑树
  - 标签：DGA分析、三比值法、DPM分析、PRPD分析等

- **其他示例**：
  - "变压器过热故障诊断"（active状态）
  - "断路器操作机构故障诊断"（draft状态）

**实现细节**：
```typescript
// 数据存储
const faultTrees = ref<FaultTreeData[]>([]);

// 加载数据
const loadFaultTrees = () => {
  loading.value = true;
  setTimeout(() => {
    faultTrees.value = generateSampleTrees(); // 生成demo数据
    loading.value = false;
  }, 500);
};

// 保存故障树
const handleSaveTree = async () => {
  if (editMode.value === 'create') {
    currentEditTree.value.metadata.id = generateId();
    faultTrees.value.push(currentEditTree.value);
  } else {
    const index = faultTrees.value.findIndex(
      t => t.metadata.id === currentEditTree.value!.metadata.id
    );
    faultTrees.value[index] = currentEditTree.value;
  }

  editDialogVisible.value = false;
};

// 删除故障树
const handleDeleteTree = async (tree: FaultTreeData) => {
  await ElMessageBox.confirm('确定要删除吗？删除后无法恢复。');
  const index = faultTrees.value.findIndex(t => t.metadata.id === tree.metadata.id);
  faultTrees.value.splice(index, 1);
};
```

---

### 3.5 诊断历史模块

#### 3.5.1 功能描述

查看、搜索和管理所有已保存的故障诊断记录。**数据存储在localStorage中。**

#### 3.5.2 当前实现

**页面组件**：`src/views/DiagnosisHistory.vue`
**数据存储**：localStorage（key: `diagnosisHistory`）
**数据格式**：JSON数组

#### 3.5.3 详细功能

**FR-501 历史记录列表**
- **表格列**：
  - 展开按钮（查看详细参数）
  - 故障类型（带颜色标签）
  - 诊断详情（支持overflow tooltip）
  - 诊断时间（格式化本地时间）
  - 操作按钮（查看/删除）

- **展开内容**：
  - 诊断输入参数
  - 以描述列表形式展示
  - 2列布局

- **故障类型颜色**：
  - 包含"过热"：danger红色
  - 包含"放电"：warning橙色
  - 其他：info蓝色

**FR-502 搜索功能**
- **搜索范围**：
  - 故障类型
  - 诊断详情

- **搜索特性**：
  - 实时搜索（输入即过滤）
  - 不区分大小写
  - 清空按钮
  - 搜索图标提示

**FR-503 历史记录详情**
- **触发方式**：
  - 点击"查看"按钮
  - 打开右侧抽屉

- **展示内容**：
  - 诊断结果：
    - 故障类型（带颜色标签）
    - 置信度（百分比显示）
    - 诊断详情
    - 建议措施（警告框显示）
  - 诊断输入参数：
    - 2列描述列表
    - 所有输入参数

**FR-504 记录管理**
- **删除单条记录**：
  - 点击"删除"按钮
  - 无二次确认
  - 从localStorage移除
  - 成功提示

- **清空所有记录**：
  - 点击"清空所有记录"按钮
  - 二次确认对话框
  - 清空localStorage
  - 成功提示

**FR-505 空状态处理**
- **无记录时**：
  - 显示Empty组件
  - 提示"暂无历史记录或匹配结果"

**数据结构**：
```typescript
interface HistoryRecord {
  id: string;              // 唯一标识
  timestamp: string;       // 诊断时间（ISO格式）
  params: any;             // 输入参数对象
  result: {
    fault_type: string;    // 故障类型
    confidence: number;    // 置信度（0-1）
    diagnosis: string;     // 诊断详情
    recommendation: string; // 建议措施
  };
}
```

**实现细节**：
```vue
<script setup>
const history = ref<HistoryRecord[]>([]);

const loadHistory = () => {
  try {
    const data = localStorage.getItem('diagnosisHistory');
    history.value = data ? JSON.parse(data) : [];
  } catch (e) {
    ElMessage.error('加载历史记录失败');
    history.value = [];
  }
};

const deleteRecord = (id: string) => {
  history.value = history.value.filter(record => record.id !== id);
  localStorage.setItem('diagnosisHistory', JSON.stringify(history.value));
  ElMessage.success('该条记录已删除');
};

const clearHistory = () => {
  ElMessageBox.confirm('确定要清空所有历史记录吗？此操作不可撤销。')
    .then(() => {
      localStorage.removeItem('diagnosisHistory');
      history.value = [];
      ElMessage.success('所有历史记录已清空');
    });
};
</script>
```

---

### 3.6 文档管理模块

#### 3.6.1 功能描述

（占位页面，具体功能待实现）

#### 3.6.2 当前实现

**页面组件**：`src/views/DocumentManagement.vue`
**实现状态**：待开发

---

### 3.7 系统导航和路由

#### 3.7.1 路由配置

**路由定义**：`src/router/index.ts`
**路由模式**：HTML5 History模式

**路由列表**：
| 路径 | 名称 | 组件 | 说明 |
|------|------|------|------|
| / | Home | HomeView | 系统首页 |
| /diagnosis | FaultDiagnosis | FaultDiagnosis | 故障推理诊断 |
| /image-diagnosis | ImageDiagnosis | ImageDiagnosis | 图像AI诊断 |
| /fault-tree | FaultTreePreview | FaultTreePreview | 故障树预览 |
| /fault-tree-management | FaultTreeManagement | FaultTreeManagement | 故障树管理 |
| /fault-tree-generation | FaultTreeGeneration | FaultTreeGeneration | 故障树生成 |
| /history | DiagnosisHistory | DiagnosisHistory | 诊断历史 |
| /documents | DocumentManagement | DocumentManagement | 文档管理 |
| /test | TestPage | TestView | 测试页面1 |
| /test1 | Test1Page | Test1View | 测试页面2 |

#### 3.7.2 导航菜单

**导航结构**：（根据App.vue实现）
- 系统采用Element Plus的菜单组件
- 响应式导航（支持移动端折叠）
- 路由自动高亮当前页面

---

## 4. 数据管理

### 4.1 数据存储策略

#### 4.1.1 localStorage存储

| 存储Key | 数据类型 | 用途 | 生命周期 |
|---------|---------|------|----------|
| `image_diagnosis_tasks` | JSON数组 | 图像诊断任务列表 | 永久（除非手动清理） |
| `diagnosisHistory` | JSON数组 | 故障诊断历史记录 | 永久（除非手动清理） |

#### 4.1.2 内存存储

- **故障树列表**：`ref<FaultTreeData[]>`，刷新页面重置为demo数据
- **当前诊断结果**：`ref`响应式数据，页面刷新清空

### 4.2 数据同步

- **无后端同步**：所有数据仅存储在客户端
- **设备间不同步**：不同浏览器/设备数据独立
- **导出导入**：通过文件实现数据迁移（部分功能待开发）

---

## 5. 外部API集成

### 5.1 n8n故障诊断API

**端点**：`https://n8n.bd.kxsz.net:9443/webhook/power-fault-diagnosis-1`
**方法**：POST
**Content-Type**：application/json

**请求体**：
```json
[{
  "H2_ppm": 150,
  "CH4_ppm": 60,
  "C2H6_ppm": 20,
  "C2H4_ppm": 50,
  "C2H2_ppm": 150,
  "CO_ppm": 100,
  "CO2_ppm": 400,
  "total_hydrocarbons_limit": 150
}]
```

**响应**：纯文本格式（非JSON），需前端解析

**错误处理**：
- HTTP状态码非2xx：显示错误信息
- 网络错误：提示用户检查连接
- 解析失败：提示诊断失败

### 5.2 n8n图像诊断API

**端点**：`https://n8n.bd.kxsz.net:9443/webhook/upload-image`
**方法**：POST
**Content-Type**：multipart/form-data

**请求参数**：
```
file: File对象
device_id: string
device_type: string
location: string
image_type: string (PRPD/UHF/TEV/AE)
sensitivity: string (low/medium/high)
file_index: number
```

**响应**：
- Content-Type可能是JSON或文本
- 前端自动判断并解析

**错误处理**：
- 上传失败：创建failed状态任务
- 网络错误：显示错误信息
- 超时：支持重试机制

---

## 6. 非功能需求

### 6.1 性能需求

**响应时间**：
- 页面加载：≤3秒
- n8n API调用：取决于网络和服务器
- 故障树渲染：≤2秒（1000节点）
- localStorage读写：≤100ms

**浏览器要求**：
- Chrome ≥90
- Edge ≥90
- Firefox ≥88
- Safari ≥14

### 6.2 兼容性

**设备支持**：
- 桌面端（Windows/macOS/Linux）
- 平板设备（iPad）
- 最小宽度：1280px（响应式设计）

**localStorage限制**：
- 存储空间：约5-10MB（根据浏览器）
- 建议定期清理历史数据

### 6.3 可用性

**用户体验**：
- 加载状态指示（v-loading）
- 操作反馈（ElMessage提示）
- 错误提示（ElMessageBox）
- 空状态处理（ElEmpty）

**帮助和提示**：
- Tooltip悬停提示
- Placeholder占位文本
- 表单验证错误提示

### 6.4 安全性

**前端安全**：
- 表单输入验证
- XSS防护（Vue自动转义）
- localStorage数据JSON验证

**API调用**：
- HTTPS加密传输
- 错误信息不暴露敏感数据

---

## 7. 技术实现

### 7.1 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.5.18 | 核心框架（Composition API） |
| TypeScript | 5.8.0 | 类型系统 |
| Vite | 7.0.6 | 构建工具 |
| Element Plus | 2.10.5 | UI组件库 |
| @element-plus/icons-vue | 2.3.1 | 图标库 |
| ECharts | 5.6.0 | 数据可视化 |
| vue-echarts | 7.0.3 | ECharts Vue封装 |
| Pinia | 3.0.3 | 状态管理 |
| Vue Router | 4.5.1 | 路由管理 |
| PDF.js | 5.4.54 | PDF处理 |
| xlsx | 0.18.5 | Excel处理 |

### 7.2 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| ESLint | 9.31.0 | 代码检查 |
| Prettier | 3.6.2 | 代码格式化 |
| Vue DevTools | 8.0.0 | 调试工具 |
| TypeScript Compiler | 5.8.0 | 类型检查 |

### 7.3 项目结构

```
fault-diagnosis-system/
├── public/                      # 静态资源
│   └── favicon.ico
├── src/
│   ├── assets/                  # 静态文件
│   │   ├── base.css
│   │   ├── logo.svg
│   │   └── main.css
│   ├── components/              # Vue组件
│   │   ├── common/              # 通用组件
│   │   │   ├── ErrorBoundary.vue
│   │   │   ├── LoadingComponent.vue
│   │   │   ├── SmartForm.vue
│   │   │   └── SmartTable.vue
│   │   ├── diagnosis/           # 诊断组件
│   │   │   ├── DiagnosisReport.vue
│   │   │   ├── EnhancedDiagnosisReport.vue
│   │   │   └── StatusCard.vue
│   │   ├── icons/               # 图标组件
│   │   ├── image/               # 图像诊断组件
│   │   │   ├── ImageUploader.vue
│   │   │   ├── ResultDisplay.vue
│   │   │   └── TaskManager.vue
│   │   └── logic/               # 逻辑门组件
│   │       ├── EnhancedFaultTree.vue
│   │       ├── LogicGateIcon.vue
│   │       └── LogicGateNode.vue
│   ├── composables/             # 组合式函数
│   │   ├── useForm.ts
│   │   ├── useLoading.ts
│   │   └── useTableData.ts
│   ├── constants/               # 常量定义
│   │   └── index.ts
│   ├── router/                  # 路由配置
│   │   └── index.ts
│   ├── services/                # API服务层
│   │   ├── diagnosis.ts         # 故障诊断服务
│   │   ├── imageDiagnosis.ts    # 图像诊断服务
│   │   ├── mockApi.v2.ts        # Mock API
│   │   ├── visualization.ts     # 可视化服务
│   │   └── workflowParser.ts    # 工作流解析
│   ├── stores/                  # Pinia状态管理
│   │   └── counter.ts
│   ├── types/                   # TypeScript类型
│   │   └── index.ts
│   ├── utils/                   # 工具函数
│   │   ├── diagnosisTools.ts    # 诊断工具
│   │   ├── faultTreeParser.ts   # 故障树解析
│   │   ├── logicGateRenderer.ts # 逻辑门渲染
│   │   └── textParser.ts        # 文本解析
│   ├── views/                   # 页面组件
│   │   ├── AboutView.vue
│   │   ├── DiagnosisHistory.vue
│   │   ├── DocumentManagement.vue
│   │   ├── FaultDiagnosis.vue
│   │   ├── FaultTreeGeneration.vue
│   │   ├── FaultTreeManagement.vue
│   │   ├── FaultTreePreview.vue
│   │   ├── HomeView.vue
│   │   ├── ImageDiagnosis.vue
│   │   ├── Test1View.vue
│   │   └── TestView.vue
│   ├── App.vue                  # 根组件
│   └── main.ts                  # 应用入口
├── app.py                       # Gradio部署文件
├── package.json                 # 项目配置
├── tsconfig.json                # TypeScript配置
├── vite.config.ts               # Vite配置
└── README.md                    # 项目说明
```

### 7.4 构建和部署

**开发环境**：
```bash
npm run dev          # 启动开发服务器（端口5173）
```

**生产构建**：
```bash
npm run build        # 构建到dist/目录
npm run preview      # 预览生产构建
```

**代码质量**：
```bash
npm run type-check   # TypeScript类型检查
npm run lint         # ESLint检查并修复
npm run format       # Prettier格式化
```

---

## 8. 限制和注意事项

### 8.1 数据持久化限制

1. **故障树管理**：
   - 数据仅存储在内存中
   - 刷新页面后重置为初始demo数据
   - 不支持跨设备同步

2. **localStorage限制**：
   - 存储空间有限（约5-10MB）
   - 清除浏览器数据会丢失所有记录
   - 不同浏览器数据不共享

3. **API依赖**：
   - 故障诊断和图像分析依赖n8n服务可用性
   - 网络问题会导致功能不可用
   - 无离线模式

### 8.2 功能限制

1. **权限管理**：
   - 系统无用户登录功能
   - 无权限控制机制
   - 所有功能对所有访问者开放

2. **数据导出**：
   - 故障树导出功能为占位符
   - 批量导出功能待实现
   - 报告导出功能待实现

3. **历史记录**：
   - 故障诊断历史需手动保存（功能未见）
   - 图像诊断历史自动保存到localStorage
   - 无历史记录分析功能

### 8.3 浏览器兼容性

- 需要支持ES6+的现代浏览器
- 需要启用JavaScript
- 需要支持localStorage
- 需要支持Fetch API

---

## 9. 未来扩展方向

### 9.1 功能扩展

1. **后端集成**：
   - 实现真实的后端API
   - 数据库持久化存储
   - 用户认证和授权

2. **功能完善**：
   - 完成文档管理模块
   - 实现导出/导入功能
   - 增加历史数据分析
   - 趋势图表展示

3. **性能优化**：
   - 大数据量优化
   - 虚拟滚动
   - 懒加载优化

### 9.2 技术优化

1. **状态管理**：
   - 完善Pinia Store使用
   - 全局状态统一管理

2. **代码质量**：
   - 增加单元测试
   - E2E测试
   - 代码覆盖率提升

---

## 10. 附录

### 10.1 术语表

| 术语 | 说明 |
|------|------|
| DGA | 溶解气体分析（Dissolved Gas Analysis） |
| PRPD | 相位分解局部放电（Phase Resolved Partial Discharge） |
| UHF | 超高频检测（Ultra High Frequency） |
| TEV | 暂态地电压（Transient Earth Voltage） |
| AE | 声发射（Acoustic Emission） |
| DPM | 杜瓦尔三角形/五边形法 |
| n8n | 开源工作流自动化工具 |
| SPA | 单页应用（Single Page Application） |

### 10.2 参考资料

- Vue 3官方文档：https://vuejs.org/
- Element Plus文档：https://element-plus.org/
- ECharts文档：https://echarts.apache.org/
- TypeScript文档：https://www.typescriptlang.org/

---

## 变更历史

| 版本 | 日期 | 修订人 | 变更说明 |
|-----|------|--------|---------|
| v1.0 | 2026-01-22 | Claude Code | 初始版本（理想化需求） |
| v2.0 | 2026-01-22 | Claude Code | 基于实际代码重写，移除权限管理等未实现功能，补充实际实现细节 |

---

**文档结束**
